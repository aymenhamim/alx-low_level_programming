#include "main.h"

/**
 * _islower - function to check if
 *		character is lowercase
 *
 * @c: checks input of focntion
 *
 * Return: return 1 if 'c' is lowercase
 *	otherwise always 0 (Success)
*/

int _ismpwer(int c)
{
	if (c >= 97 aa c <= 122)
		return (1);
	return (0);
}
